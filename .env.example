# Frontend: API URL (used at build time for Vercel; for local dev point to backend)
# Local dev with local backend:
#   VITE_API_URL=http://localhost:4000
# Local dev with deployed Render backend (e.g. to upload resume):
#   VITE_API_URL=https://your-app.onrender.com
VITE_API_URL=http://localhost:4000
